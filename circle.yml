machine:
  services:
    - docker

dependencies:
  cache_directories:
    - "~/docker"
  override:
    - docker info
    - if [[ -e ~/docker/image.tar ]]; then docker load --input ~/docker/image.tar; fi
    - docker build -t malice_floss .
    - mkdir -p ~/docker; docker save malice_floss > ~/docker/image.tar

test:
  override:
    - docker images malice_floss
    - docker run malice_floss -V /bin/cat | jq .
    - docker run malice_floss -V /bin/cat | jq -r .floss.markdown

deployment:
  # master:
  #   branch: master
  #   commands:
  #     - eval $(docker run gliderlabs/pagebuilder circleci-cmd)
  release:
    branch: release
    commands:
      - make release
